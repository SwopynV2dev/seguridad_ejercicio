installar los siguiente para usutilizar django 

linux 
python -m pip install Django==5.0.6

windows 
py -m pip install Django==5.0.6

pip install django-cors-headers

pip install djangorestframework
pip install markdown      
pip install django-filter

pip install djangorestframework-simplejwt
  
Necesitan hacer mmigracion 
python 
python manage.py migrate

python 3 
python3 manage.py migrate

crear usuario

python manage.py createsuperuser

python3 manage.py createsuperuser





Instalar ReactNative

- Instalar nodejs
Va a descargar el ejuctable y lo instalas como cualquier otro programa
https://nodejs.org/en/

- Instalar 
npm install -g react-native-cli

- Instalar StyledComponents para los estilos css
npm install styled-components

- Crear un programa en reactNative 
npx create-react-app elnombrequequeramos